
# Tipo list - Mutavel
# Suporta varios valores de qualquer tipo
# cinhecimentos reutilizaveis - indices e fatiamento

# Metodos uteis:
# append - Adiciona um item ao final
# insert - Adiciona um item no indice escolhido
# pop - remove do finql ou do indice escolhido
# del - apaga um indice
# clear - limpa a lista
# extend - estende a lista
# + - concatena a lista
# Create Read Update Delete
# Criar  Ler  Alterar Apagar = lista[1] (CRUD)

# A concatenaçao de listas pode ser feita de duas maneiras sendo elas:

#  1 - listaZ = listaX + ListaY
#  2 - listaX.extend(ListaY)

# Podemos "empacortar" uma lista e desempacotar apenas os
# produtos que desejamos utilizando:

# lista = ['Pedro', 'Amanda', 'josue', 'Angela', 'Emanuela']
# _, nome1, *_ = lista

# O _ para indicar que a variavel nao sera utilizada,
# porem tambem podemos dar nome a esta variavel por exemplo: 

# resto, nome1, *resto = lista 

# tambem podemos utilizala para pegar somente o primeiro e o
# ultimo item da lista ex:

# nome1, nome2, *_, nome9, nome10 = lista